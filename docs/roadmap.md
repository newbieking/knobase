# 知序 Knobase 迭代路线图

> 周期：4 周短期冲刺 · 方向：RAG 能力深化
> 制定日期：2026-09-17
> 最近计划审查：2026-09-22；S3.0 三态契约代码已落地（生成基线待授权采集），S3.1 及之后为待确认方案。
> 2026-09-23：模型调用由 `urlopen` 迁移到 LangChain / openai SDK 封装（见 S2.2 末段），重排只保留百炼原生协议、地址原样使用。

---

## 当前审查结论（2026-09-22）

本次只核对计划、源码与已有评测记录，没有运行测试、构建或真实网关评测；历史实测不代表本次重新验收。

- **S1/S2 检索主线已有落地记录，不能等同于生成质量闭环。** S2.2 的真实网关 paraphrase
  `docHit@5` 为 0.930，相对 S1 的 0.605 提升 32.5pp；离线基线继续独立保留。
- **拒答指标需要分层。** `eval/run_eval.py:79-105` 只调用检索与本地摘录，`:146-155`
  的 refusal / falseRefusal 仅判断是否返回片段。0.500 → 0.167 是检索层退化，不能据此声称
  LLM 拒答率，也不能用离线 CI 通过证明真实网关的生成质量。
- **S3.0 已落地终态契约（代码）。** 原 `app.py` 的"答案必须含合法引用"会把有候选时模型无引用的合理
  拒答误判成 502；现由 `[[REFUSAL:reason]]` 哨兵把拒答判为 `outcome=refusal`，正常 `answer` 路径仍要求
  合法引用、未放宽，空响应/越界引用/原因非法仍报错。生成的语义正确性仍需 S3.0 基线采集与 S4 忠实度校验。
- **流式与后验校验存在冲突。** S4 的“校验失败后重试/替换”无法撤回用户已看到的 delta。
  建议 S3 将增量正文标为草稿，以最终确认结果替换；这不保证草稿事实正确，严格禁止展示未校验内容
  则必须缓冲，并重新讨论首字延迟目标。
- **事务计划需修正。** `WorkspaceService.java:166-200` 已在 AI 返回后才开短事务写问答、活动与统计。
  S3 应复用此边界，新增流式生命周期管理，而不是再次做“把网络调用移出事务”的重构。
- **验收前置条件尚缺。** 当前评测集没有多轮 history 契约，也没有生成引用忠实度基线；
  “首 token < 1.5s”未定义计时范围、分位数与冷/热索引条件，需在 S3 开始前明确。

建议顺序：**S3.0 终态契约与生成评测口径 → S3.1 端到端流式闭环 → S3.2 多轮改写 → S4 质量与用量闭环**。
S3.0 三态契约代码已落地；其生成评测基线待授权采集，测试/命令/付费网关调用另行授权。下一步先取生成基线或推进 S3.1。

---

## 立项时现状（2026-09-17，历史记录）

以下保留立项依据，不代表上述问题在当前版本仍全部存在；进度以各阶段结论及最新审查为准。

三层架构（React 19 + Spring Boot 3.4 + FastAPI）可跑通完整链路，文件解析端的安全边界扎实
（`backend/ai-service/app.py:149-212`），Java 集成测试与 Python 单测已就位。

核心瓶颈集中在检索层：

- **纯词面召回**。BM25 + TF-IDF 只在共享词项上打分（`app.py:315-396`），同义改写
  （"休假制度" ↔ "年假怎么请"）完全召回不到，这是企业问答准确率的第一限制因素。
- **每次问答全量重分段**。`retrieve()` 在请求内对全部候选文档重新切分（`app.py:321`），
  延迟随语料线性劣化，也是 500 文档 / `MAX_TEXT_CHARS` 上限存在的原因。
- **topK 设置失效**。`app.py:394` 的 `min(request.topK, 4)` 让设置项中 topK > 4 静默无效。
- **分段口径两套实现**。Java `TextChunks.split` 与 Python `chunk_document` 各自计算，必然漂移。
- **无流式输出**。`/internal/query` 一次性返回，长答案首字延迟等于总耗时。
- **无评测能力**。改动好坏只能靠主观感受判断。

**排期原则**：第 1 周先建评测基线，后续三周每个改动都用同一把尺子量化，避免"感觉变好了"。

---

## S1（第 1 周）评测基线 + 检索结构整固

| 任务 | 落点 |
|---|---|
| Golden set 100~150 条：`{问题, 期望文档/页, 期望要点, 应拒答}`，取材 `SeedData.java` 的 6 库 24+ 文档 | `backend/ai-service/eval/dataset.jsonl` |
| 指标脚本：检索侧 recall@k / MRR / nDCG；端到端侧引用正确率、越界引用率、无据拒答率 | `eval/run_eval.py` + markdown 报告 |
| 修复 topK 上限，让设置真正生效 | `app.py:394` |
| 分段口径统一：索引侧以 Python 为准，Java 只持久化片段数 | `TextChunks.java` / `app.py` |
| CI 三条 job（`tsc -b && vite build`、`mvn verify`、`python -m unittest discover` + 评测回归门；仓库原无 `.github/`） | `.github/workflows/ci.yml` |

**DoD**：基线数字入库（`docs/eval-baseline.md`），CI 绿。

### S1 结论（2026-09-17 实测）

golden set 实际铺到 **192 条**（8 类：exact 97 / paraphrase 43 / no_answer 20 / numeric 15 /
cross_doc 7 / distractor 4 / scope 4 / latin 2），比原定 100~150 多，因为拒答与改写两类
各自要够样本量才能分开看。全量基线：

| 指标 | 数值 | 指标 | 数值 |
|---|---|---|---|
| docHit@1 | 0.738 | docHit@5 | 0.857 |
| ndcg@10 | 0.806 | passageRecall@5 | 0.848 |
| refusal | 0.500 | falseRefusal | 0.119 |
| answerCoverage | 0.348 | 检索耗时 | 均值 12 ms / P95 37 ms |

topK 与分段口径均已收敛到单一实现：`app.py:394` 的隐藏上限去除后设置真正生效；Java 侧
`TextChunks.java` 删除，片段数改由 `POST /internal/chunk` 用同一个分词器算，seed 文档正文
移到 `business-service/src/main/resources/seed/documents.json`，评测器与业务共用这一份语料
（`--check-corpus` 守住片段数不漂移）。

四条改判 S2 的发现：

1. **改写类是唯一的大坑**：exact 0.959 vs paraphrase **0.605** docHit@5，latin 0.500。
   差距就是 S2 语义召回的预算依据，也给出可量化目标——把 paraphrase 拉到 0.75 以上。
2. **片段级指标已经退化**：LlamaIndex `SentenceSplitter` 按 **token** 而非字符计数，
   "512" 下 24 篇文档只切出 26 个片段，于是 docHit@5 == docHit@10、passageRecall@5 == @10。
   S2 建持久索引前必须先把 `chunkSize` 口径改成字符（或明显更小的默认值），否则任何
   片段级调参都没有分辨率。
3. **拒答基本没在工作**：no_answer / scope 两类 refusal 都只有 **0.500**，24 条应拒答问题里
   12 条仍返回引用。当前判据是"最高分为 0 才拒"，词面检索对无关问题几乎总能给出非零分。
   需要相对阈值（与 top 结果脱钩）或语义相似度下限，这是 S2 融合 dense 分数时一并解决的。
4. **确定性是回归门的前提**：同一份代码两次跑出 docHit@5 0.768 vs 0.857——浮点求和顺序
   跟着 set 迭代顺序（`PYTHONHASHSEED`）走。修成排序迭代后跨 seed 稳定，CI 门才有意义：
   `docHit@5 ≥ 0.80`、`ndcg@10 ≥ 0.75`、`refusal ≥ 0.45`、`falseRefusal ≤ 0.15`。

遗留：seed 文档 `doc-tech-api` 写着"Top K 范围 1 至 50"，与 Python/前端的 1~20 不一致，
属内容修正，未在本迭代内改动。

---

## S2（第 2 周）语义召回 + 索引持久化

状态：**检索主线完成，拒答风险未闭环**（2026-09-17 主体落地，2026-09-20 审查修正，
2026-09-22 级联模型重排 + 真实网关实测）。按既有实测采用的 `paraphrase docHit@5` 口径，
+10pp 目标在离线 LSI 下未达，接入真实 embedding 网关后达成（见 S2.2）；不要与 `docRecall@5` 混用。
生成拒答的协议问题拟前移至 S3.0，语义正确性仍由 S4 验收，不继续拟合向量阈值。
下面保留立项时计划原文，与实测结论有出入处以 [S2 结论](#s2-结论2026-09-17-实测)、
[S2.1](#s21-审查修正2026-09-20) 与 [S2.2](#s22-级联模型重排与真实网关实测2026-09-22) 为准。

- `chunkSize` 改成按字符计（或把默认值降到能产生分辨率的量级）。当前 LlamaIndex 按 token 计数，
  24 篇文档在 512 下只有 26 个片段，片段级指标 @5 与 @10 完全相等，调参没有分辨率。
- 拒答判据从"最高分为 0"换成相对阈值（与 top1 分数或分数分布脱钩）。S1 实测 no_answer / scope
  两类 refusal 均为 0.500，24 条应拒答问题有 12 条返回了引用。
- Embedding 接入 OpenAI 兼容 `/v1/embeddings`，新增 `EMBEDDING_MODEL_ID` 配置项。
  **无 key 时保持当前词面链路，零回归。**
- 本地持久索引：SQLite + numpy 暴力检索即可，不引入独立向量服务，避免运维面扩大。
  缓存 `(doc_id, chunk_hash, vector)`，在 reindex / delete 时失效。
  顺带取消每问全量重分段，检索延迟不再随语料线性增长。
- 融合从双路扩到 BM25 + TF-IDF + dense 三路，RRF 泛化（`app.py:352-359`）。

**DoD**：同义改写类问题 recall@5 相对基线 +10pp 以上；`/health` 与前端健康 badge 反映索引就绪状态。

### S2 结论（2026-09-17 实测）

已落地：`chunkSize` 改按字符计（512 字符下 24 篇文档 = 79 片段，此前 512 token 只有 26 片段）；
LSI 潜在语义路 + BM25/TF-IDF/dense 三路加权 RRF；SQLite 本地索引缓存分段、词频与向量；
可选 OpenAI 兼容 embedding 网关（`EMBEDDING_API_KEY/MODEL_ID/BASE_URL`），未配置即静默走 LSI。

| 指标 | 词面链路 | + 语义三路 |
|---|---|---|
| docHit@1 | 0.732 | 0.744 |
| docHit@5 | 0.857 | **0.881** |
| passageRecall@5 | 0.839 | **0.866** |
| ndcg@10 | 0.779 | 0.797 |
| paraphrase docHit@5 | 0.605 | **0.674** |
| refusal / falseRefusal | 0.500 / 0.119 | 0.500 / 0.119 |
| 单查询耗时（评测口径 均值 / P95） | 2.4 / 7.8 ms | 6.9 / 32 ms |

nDCG 改为累计新增命中计增益（重复片段不再重复加分），与 S1 的 0.806 不可直接比较。

**拒答判据的结论：LSI 分数撑不起相对阈值。** 词面零证据的 12 条应拒答与 19 条可回答用例，
LSI top1 分数区间 0.64–0.97 / 0.46–0.94、标准化离差 z 区间 1.90–4.25 / 1.59–2.80，完全重叠；
且 z 与"语义邻域宽度"随知识库片段数（13 段 vs 79 段）系统性漂移——任何统一阈值都只是拟合当前语料规模。
因此 dense 路只在词面已有证据时参与排序与召回；词面完全没有证据时，仅允许**模型校准过的向量相似度**
（`VECTOR_EVIDENCE_FLOOR = 0.35`）单独召回，LSI 不享有这个权利。该阈值需在接入真实 embedding 模型后重新校准。

**DoD 未达成项**：paraphrase docHit@5 +6.9pp（0.605 → 0.674），未到 +10pp。剩余缺口全部卡在
词面零证据的守卫上——LSI 已把其中 13 条的目标文档排到第 1 名，但没有可信的绝对分数放行它们。
本机配置的 DeepSeek 网关没有 `/v1/embeddings` 端点，向量路只能以 mock 网关验证行为（`test_app.EmbeddingTests`），
无法在评测集上取数。**接入任一支持 embeddings 的网关即可完成，代码路径已就绪。**

索引缓存的收益在本语料规模下主要体现在重复计算被消除（分段/分词只在内容哈希变化时重算），
6.9 ms 里大头是每次请求的 SVD 拟合；语料变大后由 `SEMANTIC_MAX_CHUNKS` 截断退回词面链路。
（表中耗时按 S2.1 修正后的评测口径重测：旧口径 10 / 17.8 ms 含评测 runner 对同一查询的重复检索。）
CI 回归门随本次基线上调为 `docHit@5 ≥ 0.86`、`ndcg@10 ≥ 0.78`、`passageRecall@5 ≥ 0.85`，
`refusal ≥ 0.45` / `falseRefusal ≤ 0.15` 保持不动。

### S2.1 审查修正（2026-09-20）

对 S2 提交做了双轴审查（规范符合性 + 工程标准），确认并修复以下正确性缺陷，行为变更均不推翻
S2 的架构决策：

- **向量证据门槛只看榜首**：门槛之前只校验 dense 榜首，其余候选即使相似度为 0 甚至 −1 也照样
  进入融合（复现时相似度为 0 与 −1 的片段被一并引用）。改为逐条片段过滤：校准向量路
  `score >= VECTOR_EVIDENCE_FLOOR`，LSI 路 `score > 0`（负相关与零分不构成证据）。
- **embedding 响应忽略 `index` 字段**：网关乱序返回时向量会错位配对。改为按 `index` 归位，
  非整数/越界/重复索引直接判定响应不可用。
- **跨批次维度漂移**：缓存与网关返回维度不一致会抛未捕获的 `ValueError`。改为整批校验
  （维度唯一、有限值、非零范数），不通过则降级 LSI 并告警，且只有校验通过才写缓存，
  异常响应不再污染索引。
- **半份分段缓存被当作命中**：行级淘汰或写入中断后 `chunks()` 仍返回残缺集合，出现"冷查询命中、
  热查询拒答"。新增 `chunk_set` 清单表，读回要求清单匹配且行数一致；淘汰改为整篇文档最早优先，
  单文档超容量不落缓存。
- **删除/重建索引不清理检索缓存**：新增 `POST /internal/index/purge`，业务服务在文档删除、
  知识库级联删除、重建索引提交事务后调用（失败仅记日志：缓存键是内容哈希，残留行不会再被读到）。
- **健康状态说配置不说实际**：`/health` 的 `semantic` 之前只看是否配置了网关。改为报告最近一次
  检索实际使用的路由，网关配置了但调用失败时如实显示 `lsi`；前端 badge 同步区分"内存模式"。
- **测试与评测隔离**：单元测试改为显式清空 `EMBEDDING_*` 环境变量（此前继承宿主机配置，
  装好网关的机器上会走真向量路）；评测 runner 修正了同一次评测里检索跑两遍的问题；
  Java 集成测试桩补上 purge 路由并断言删除/重建索引确实通知了检索侧。
- **CI 增加复述类单列门槛**（`--only paraphrase --min docHit@5=0.64`）：综合门槛会被 exact 类的
  高分掩盖，语义路静默失效不会报警。阈值取在 S1 词面基线（0.605）与 S2 实测（0.674）之间。

未修复的仍是同一条：DoD 的 paraphrase +10pp 卡在可信 embedding 网关上，代码路径与 mock 行为
验证已就绪（`test_app.EmbeddingTests`），接入任一支持 `/v1/embeddings` 的网关后需重新取数、
校准 `VECTOR_EVIDENCE_FLOOR` 并刷新 `docs/eval-baseline.md`。
（2026-09-22 更新：真实网关已接入并完成取数，见下方 S2.2；阈值校准经实测判定不可分，延后至 S4。）

### S2.2 级联模型重排与真实网关实测（2026-09-22）

**级联重排落地**：`rerank_candidates()` 在本地混合排序（`0.70/0.22/0.08`）之后取 Top-30
（`RERANK_CANDIDATES`）交给模型打分，只保留一种协议：DashScope 原生 text-rerank——请求体
`input.query` / `input.documents` / `parameters.top_n`，响应取 `output.results[].relevance_score`；
`RERANK_BASE_URL` 是完整地址，原样使用、不做路径补全（OpenAI 官方没有 `/v1/rerank`，LangChain 也没有现成
Rerank 集成，自定义的 OpenAI 兼容分支已于 2026-09-23 删除）。响应按 `index` 归位并逐条校验（索引唯一且不越界、
分值 ∈ [0,1]），任何一处不合法即整单回落到本地序并告警。模型排序成功后不再混入未评分的尾部候选：
两阶段分数尺度不可比。`RERANK_*` 未配置时行为与 S2.1 完全一致。

**模型调用统一走 LangChain 封装（2026-09-23）**：删除 `urllib.request.urlopen`——生成路本就是 `ChatOpenAI`，
嵌入路改为 `GatewayEmbeddings`（`langchain_core.embeddings.Embeddings`，内部 openai SDK），
重排路改为 `GatewayReranker`（`langchain_core.documents.compressor.BaseDocumentCompressor`，内部 httpx）。
`encoding_format="float"` 必须显式传给 SDK（默认 base64，非 OpenAI 网关未必支持）。索引归位、重复/越界拒绝、
维度校验与整单回落语义不变；重排的地址与请求/响应形状未变，现有配置无需迁移。

**迁移后实测（2026-09-23）**：`python -m unittest` 58 项全通过（13.1s）；真实网关执行一次
`/internal/query`（24 篇种子文档 / `chunkSize` 512 / LLM `deepseek-flash`）返回 `outcome=answer`，
`/health` 的 `semantic=gateway`、服务日志无重排回落告警、`usage` 透传 1738 token，耗时 4.8s。
本次语料向量已命中本地缓存，真实网关只跑了 1 条文本的单批嵌入；满批（25 条）路径仅由离线用例覆盖。

`EMBEDDING_BATCH_SIZE` 由 64 改为 25：阿里云百炼兼容模式单批最多 25 条文本，超限整批 400
会让语义路静默回落到 LSI。

**真实网关实测**（192 用例 / 24 文档 / 79 段；向量 `qwen3.7-text-embedding-flash` 1024 维 +
重排 `qwen3.7-text-rerank`）：

| 指标 | S2 离线基线（LSI） | 真实服务（向量 + 级联重排） |
|---|---|---|
| docHit@1 | 0.744 | 0.958 |
| docHit@5 | 0.881 | 0.976 |
| passageRecall@5 | 0.866 | 0.967 |
| ndcg@10 | 0.797 | 0.958 |
| paraphrase docHit@5 | 0.674 | 0.930 |
| refusal / falseRefusal | 0.500 / 0.119 | 0.167 / 0.012 |
| 单查询耗时（均值 / P95） | 6 / 32 ms | 321 / 485 ms |

S2 的 DoD 在真实网关下达成：paraphrase docHit@5 0.605（S1 词面）→ 0.930。级联重排的增量集中在
排序质量——复述类 docHit@1 由 0.721 抬到 0.884、ndcg@10 由 0.783 抬到 0.885，docHit@5 保持 0.930
（重排改善排序，不改召回上限）。耗时从 6 ms 升到 321 ms 是每次查询都要调用网关的代价。
`docs/eval-baseline.md` 保持离线口径不刷新，CI 门不受影响。

**拒答判据的结论：真实向量的绝对相似度同样分不开应拒答与可回答。** 12 条词面零证据的应拒答
用例，向量 top1 落在 0.297–0.451（中位 0.396）；同条件下可回答用例命中 gold 的 top1 落在
0.296–0.641（中位 0.426），两者在 `VECTOR_EVIDENCE_FLOOR = 0.35` 附近几乎完全重叠。改用
「top1 − top2 间隔」也分不开（应拒答 最小/中位/最大 = 0.000/0.030/0.068，可回答 = 0.001/0.035/0.238）。
任何阈值都只是拟合当前语料，**校准决定延后到 S4**：拒答改由生成侧对幻觉诱导问句兜底，检索层
不再调参；本轮 refusal 0.500 → 0.167 的临时回退按已知项记录。

---

## S3（第 3 周）终态契约 + 流式输出 + 多轮检索改写

状态：**待确认、未实施**。按 S3.0 → S3.1 → S3.2 分批推进；每批独立审查，不承诺在未实测前满足延迟目标。
下面的文件行号指本次审查时的实现入口，不是已发生的修改。

### S3.0 下一步：先明确回答终态与验收口径

1. **回答与拒答分开处理** — `backend/ai-service/app.py:134-139、991-1063`。
   模型输出采用可解析的结构化结果，显式区分 `answer` / `refusal`；拒答携带受限原因
   （资料不足或超出范围），不能仅凭“没引用”或拒答关键词猜测成功。正常回答仍要求合法引用；
   合理拒答允许空引用，即使检索返回了候选片段。空响应、非法引用、结构解析失败仍是错误，
   不得包装成拒答。模型自报拒答只解决协议，是否合理仍需独立生成评测。
2. **统一最终结果边界** — `AiClient.java:39、66-100`、`WorkspaceService.java:166-200`。
   内部结果增加回答状态及拒答原因；同步外部 `ChatResponse` 保留现有字段，拒答映射为完整消息。
   保留 Java 的文档白名单、引用页码/分数检查与服务端文档名称；后续流式候选引用和最终引用都要经过
   同样检查，不能把 Python 事件原样透传。只预留可空的模型 usage，不在本批改统计表或 Dashboard。
3. **冻结生成评测口径** — `backend/ai-service/eval/run_eval.py:35-50、79-105、146-166`。
   保留现有 192 条检索集及离线基线，另设生成/多轮用例集与显式评测入口；现有严格字段校验不接受
   history，需明确新用例契约，不能直接塞字段或仍只调用 `rank_chunks` 就宣称测过多轮。
   用例至少覆盖正常回答、有候选但证据不足、无候选、越界引用与历史诱导；同时包含可回答对照，
   防止“全部拒答”提高分数。先采集生成拒答率、误拒率、事实与引用支持度，不能用 `answerCoverage` 替代。

**本批验收**：有候选时的合法拒答不再误报 502；正常回答的非法引用仍被拒绝；结构化解析和同步
调用契约明确，生成基线与检索基线分开。测试用例编写、测试运行和网关取数均须另获授权；
未取数时只标记“契约就绪、生成基线待采集”，不得标为质量验收完成。

### S3.0 落地结论（2026-09-22，代码已实现，基线待采集）

**协议**：拒答用可解析的显式哨兵而非 JSON 或关键词。模型判定资料不足 / 超出范围时，只以
`[[REFUSAL:evidence_insufficient]]` 或 `[[REFUSAL:out_of_scope]]` 开头声明（原因枚举受限）。
`call_provider` 命中哨兵 → `outcome=refusal` 并清空 `citations`；未命中 → 走 `answer` 路径，
**仍要求非空且至少一个合法编号引用**，越界 / 无引用 / 空响应 / 哨兵前缀但原因非法一律 502（不放宽）。
检索零结果短路在调用模型之前，直接判 `refusal(evidence_insufficient)`、不调模型。`usage` 尽力从
langchain 响应透传，本批不落库、不进 Dashboard。

- `app.py`：`QueryResponse` 增加 `outcome` / 可空 `refusalReason` / 可空 `usage`，`model_validator`
  保证 answer 非空、拒答必带原因、正常回答不带原因；新增 `REFUSAL_*` 常量、`ProviderResult`、`extract_usage`。
- `AiClient.java`：内部 `Answer` 增加 `outcome` / `refusalReason` / `usage` 并严格校验；
  `WorkspaceService.chat()` 只读旧访问器，**外部 `ChatResponse` / `Message` 字段不变 → 前端零改动**，
  拒答照常落库为一条完整 assistant 消息、计一次成功问答。
- 契约联动（必要，非新增用例）：`test_app.py:148` 响应字段集合断言 + `BusinessApiIntegrationTest.java:436`
  mock AI 桩补 `outcome`，否则既有正常问答用例会因缺字段回归成 502。

**状态**：`/internal/query` 三态契约代码就绪。未运行测试、构建或真实网关，未新增评测脚本 / 用例 / 取数。
生成评测基线待授权采集后方可验收。冻结的生成 / 多轮用例 schema（独立于 192 条检索集，显式入口，
现有严格 `CASE_FIELDS` 不塞 history）：`{id, question, history?:[{role,content}], kind,
expectedOutcome: answer|refusal, expectedReason?: evidence_insufficient|out_of_scope,
goldDocs?, mustContain?, mustNotContain?}`；覆盖矩阵须含正常回答、有候选但证据不足、无候选、越界引用、
历史诱导，并配可回答对照防“全拒答刷分”。指标：生成正确拒答率 / 误拒率 / 有据事实占比 / 引用支持度，
不以 `answerCoverage` 或检索 refusal 替代。

### S3.1 再做端到端流式闭环

- **端点**：Python 新增 `POST /internal/query/stream`；Java 新增 `POST /api/chat/stream`
  （`ApiController.java:74-75`、`AiClient.java:123-154`）。保留 `/internal/query` 和 `/api/chat`
  的本地、模型两种模式；`src/Workspace.tsx:27` 的 Analytics 仍使用同步接口，不在本批迁移。
- **事件契约**：成功路径 `meta` → `citations` → `delta`（可多次）→ `done`；失败以 `error` 终止。
  请求校验在发流前完成，仍使用 HTTP 错误；流开始后的失败只能走事件。EOF 不等于成功，不允许重复终态。
  `meta` 携带本次请求标识与模式；`citations` 是候选来源，不是已核实证据。
  `delta` 只含可展示正文，不泄露模型结构化封装；`done` 携带最终正文、最终引用、回答状态、耗时及可空 usage。
  Java 侧 `done` 还包含已提交的 `sessionId` 与 `message`，前端以此整体替换草稿。
- **生成策略**：使用真正的上游流式读取，不把同步完整答案拆成字符伪装流式。无 key 时用同一事件生命周期
  返回本地摘录；无证据时返回拒答终态。结构化输出解析复用 S3.0 语义，草稿不代表通过最终检查；
  校验失败清除草稿并报错，S4 才加入语义校验与重试。不要为隐藏网关失败而在流中悄悄切换模式。
- **提交与取消**：复用现有短事务，在完整终态检查通过后原子写入问答、活动及成功统计，再发客户端 `done`；
  网络转发不占用事务。错误、半截流和提交前已确认取消的请求不保存问答。取消与提交必须有单一状态裁决；
  若提交已开始或已成功，不承诺回滚，提交后断连可能留下完整结果，但不能留下半条消息。
  终态未收到时前端提示结果未确认并刷新历史，不自动重发，避免重复问答。
- **资源边界**：`AbortController` → Java 响应流 → Python/上游生成调用逐层关闭；不能只取消
  `asyncio.to_thread` 的等待就宣称上游已停止。明确连接、流空闲与总耗时上限，必要时发 SSE 注释心跳；
  使用受限执行资源和输出缓冲，慢客户端/超时应终止，不引入消息队列或长数据库锁。
- **前端落点**：`src/api.ts:1-12` 新增独立流读取，不改变原 JSON helper；`src/types.ts:5-11`
  定义事件类型；`src/Chat.tsx:23-42` 增量渲染、草稿标识、停止按钮和卸载清理。解析须覆盖跨网络块
  UTF-8、半帧/多帧、空行边界；忽略旧请求的迟到回调，完成前禁用复制与反馈。

**本批验收**：正常/本地/拒答都能结束；半截流、非法引用、超时、取消、保存失败不被当作成功；
最终展示与落库消息一致，已删除会话不被恢复；切页后无迟到内容串入新会话。授权后启动服务并在浏览器
检查完整问答、停止、引用查看、历史恢复及同步 Analytics 回归，不以类型检查替代 UI 验收。

### S3.2 最后接入多轮检索改写

- `backend/ai-service/app.py:96-105、1001-1011、1046-1056`：有历史且配置 LLM 时增加一次改写，
  **检索用改写串，生成和落库用原问**。改写与生成共用最近 6 条、6000 字符历史预算；历史不是事实来源。
  无历史/无 key 跳过，改写超时或结果无效回原问，并记录实际路径与耗时。改写结果遵守问题长度限制。
- 改写不得修改 Java 选定的知识库范围、候选文档或设置；范围切换后，即使历史提到其他库，
  也只能引用本次选定资料。会话并发沿用“请求时历史快照、按完成顺序追加”，不承诺并发请求互相可见；
  `WorkspaceRepository.java:147-162` 的行锁只保护追加，不能当作多轮生成串行保证。
- 独立多轮集覆盖指代、序号追问、跨轮条件、话题切换、知识库范围切换及历史诱导；每例明确历史、
  原问、期望文档/要点与应拒答标记。与同一输入“不改写”对照，不用检索字符串相等作为语义正确标准。

**本批验收**：确定性的契约/范围用例全部通过；真实网关多轮检索命中与回答质量对比有记录，
不损害原单轮回归门。具体质量门槛先由 S3.0 基线确定，再冻结评测样本，不边调参边改门槛。

### S3 延迟与发布口径

- 首字从浏览器发出请求计至首个非空正文 delta，包括改写、检索、重排、生成及代理传输；meta、
  引用与心跳均不算首 token。另报最终 `done` 延迟，不能用先展示草稿证明最终可信答案已就绪。
- 保留 **首字 < 1.5s** 作为目标，拟按固定网关/模型、24 文档/79 片段、预热索引、串行请求，
  单轮与多轮各至少 30 次，分别报告 P50/P95，并以 P95 验收；冷索引和失败/降级另报。
  若目标未达，拆解耗时再调整计划，不用平均值或排除慢请求掩盖。此处尚无流式实测结论。
- **前后端契约提示**：S3.0 的 Python/Java 内部结构化响应同步更新；S3.1 的 SSE 事件在 Python、Java、
  前端三层同步实现，新前端须在新后端就绪后发布。旧同步端点及 JSON 字段不删除，不影响 Analytics。

---

## S4（第 4 周）生成质量与用量闭环

状态：**待 S3 契约稳定后实施**。模型重排已在 S2.2 完成，不重复安排“真实重排”。

- **生成侧拒答与忠实度**：在 S3.0 基线上评估 claim ↔ 引用原文的支持关系，不能只检查编号存在。
  证据不足必须拒答；有证据但生成校验失败可重试一次，仍失败才返回明确标识的原文摘录。
  摘录不是拒答，也不是正确性的兜底证明，不得把无证据问题的首段资料当作答案。
  流式沿用草稿/最终替换契约，重试结果不得拼接到旧草稿；检索层不再拟合向量阈值。
- **真实用量**：同步与流式透传供应商实际 usage，区分真实、估算、未知；涵盖改写、生成、校验及重试，
  取消/失败不写问答不代表模型没有消耗。生成 token 与 embedding/rerank 消耗分开说明，不混作一个账单。
  替换 `WorkspaceService.java:195-198` 的字符估算，并在统计中区分既有演示/估算历史与真实新增数据，
  不删除历史，也不把历史估算反标成真实。无 usage 的网关不得记作零消耗。
- **反馈持久化**：`src/Chat.tsx:17、41` 当前只有“有帮助”切换，并非完整赞/踩功能。
  先将现有反馈按已落库 messageId 保存并支持撤销；新增负反馈交互另定范围。反馈作为人工筛选的评测
  候选来源，不自动把用户反馈当作 gold，也不把当前无会话关联的 localStorage ID 盲目导入。

**拟定 DoD**：固定留出集上报告有据事实占比、引用支持度、生成正确拒答率与误拒率；
引用支持度目标为基线 +10pp（上限 100%），生成正确拒答率目标 ≥90%、误拒率 ≤10%，
须在 S3.0 取得基线后确认可行性并冻结。无基线不得宣称达标，检索 refusal 不作为生成拒答的替代指标。
Dashboard 明确区分真实与演示/估算用量，反馈在刷新和重启后保留。

---

## 本月明确不做

鉴权与多租户、H2 → MySQL/PG 迁移、Docker Compose 部署、前后端分离部署、扫描件 OCR、
新文件格式支持、答案生成缓存。

这些属于生产化基座，建议排入下一季度——若与向量索引、流式改造同期推进，会在数据模型和
请求链路上互相踩踏。

---

## 主要风险

1. **外部网关依赖**（embedding / rerank / LLM）。未配置时保持本地可用；运行时失败按能力明确处理：
   检索/重排沿用已有回退，改写回原问，生成失败不能伪装成成功或合理拒答。
   离线回归与真实网关评测分开；CI 不依赖付费网关，也不能替代真实服务质量验收。
2. **SSE 终态与提交竞态**。保留现有短事务，流转发与落库解耦；取消、超时、重复终态、
   会话删除及提交后断连需要独立验收。“客户端没收到 done”不代表服务端一定没提交。
3. **评测过拟合**。已用于调参的 192 条不能重新命名为未见留出集。新增生成/多轮样本中预留
   20% 人工改写、跨文档综合与干扰样本作为独立留出，不参与调参；基线和候选方案使用相同评分口径。
4. **排期与目标尚待实测**。四周是原始排期，不是新增结构化输出、流式解析和生成评测的工期保证。
   每批完成契约与授权验收后再推进；达不到质量或延迟目标时显式调整范围，不将任务状态提前标为完成。
